# Express Backend Development Dockerfile
FROM node:18-alpine

# 設置工作目錄
WORKDIR /app

# 複製 package 文件
COPY package*.json ./

# 安裝依賴 (包含開發依賴)
RUN npm install

# 暴露端口
EXPOSE 3001

# 啟動開發服務器
CMD ["npm", "run", "dev"]
